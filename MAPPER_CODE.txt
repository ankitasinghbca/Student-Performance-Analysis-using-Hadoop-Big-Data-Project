#!/usr/bin/env python
import sys
	
for line in sys.stdin:
    if "studentId" in line:
         continue
	
    data = line.strip().split(',')
	
    try:
        subject = data[2]
        marks = float(data[3])   # index check karo
        print(subject + "\t" + str(marks))
    except:
	continue
